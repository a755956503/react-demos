import { genComponentStyleHook, FullToken } from 'antd/es/theme/internal';

const genSizeButtonStyle = (token: FullToken<'Button'>, sizePrefixCls: string = ''): any => {
  const { componentCls } = token;
}

export default genComponentStyleHook('Button', (token) => {
  return []
});